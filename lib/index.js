/**
 * Host half of dsh-particle-whale.
 *
 * A no-op loader entry. The animation lives in the browser half (`./client`),
 * which dsh-client-modules loads from the package's `dsh.client` field.
 */
export function apply() {}
